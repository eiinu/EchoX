[package]
name = "echox"
version = "0.1.0"
description = "EchoX terminal app for macOS"
authors = ["Eiinu"]
edition = "2021"
resolver = "2"

[build-dependencies]
tauri-build = { version = "2.5.3", features = [] }

[dependencies]
tauri = { version = "2.10.0", features = ["tray-icon"] }
